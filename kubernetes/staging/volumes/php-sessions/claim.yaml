apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-sessions-claim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  volumeName: php-sessions-pv
  resources:
    requests:
      storage: 1Gi
